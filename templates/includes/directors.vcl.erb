# set director <%= @title %>
import directors;
sub directors_init {
  new <%= @title -%> = directors.<%= @type -%>();
  <%- @backends.each do |backend| -%>
  <%= @title -%>.add_backend(<%= backend -%>);
  <%- end -%>
}
